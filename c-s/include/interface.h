
#ifndef INTERFACE_H
#define INTERFACE_H
#include <stdlib.h>
#include <stdio.h>
const char *data_to_client = (char*)malloc(32 * sizeof(char));
const char *data_to_db = (char*)malloc(32 * sizeof(char));

//void text_from_DB(char);
//void text_from_S(char);


#endif /*INTERFACE*/
